# Casa del Pollo — unsubscribe microservice env values
# Copy to .env for local testing or to a private path in production.

APP_ENV=development
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=casadelpollo
DB_USER=unsubscribe_app
DB_PASS=replace-with-db-password
DB_CUSTOMER_TABLE=CUSTOMERS
DB_TEST_CUSTOMER_TABLE=TESTCUSTOMERS
CDP_UNSUB_STORAGE_DIR=/tmp/cdp-unsubscribe
